{
  endpoints: {
    new:        ["/new/:type",       "POST"],

    save:       ["/save/:type",       "POST"],
    read_list:  ["/read_list/:type/", "GET"],
    delete:     ["/delete/:type",     "GET"],

    key_exists: ["/key_exists/:type/:key",  "GET"],
    last_key:   ["/key_exists/:type/",      "GET"],
  },
}