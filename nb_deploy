P="/home/pavlo/0/services/apps/nb"

make bl


sudo service nb stop

rm ${P}/nb
mv bin/nb ${P}/nb

# TODO: use production JS build

rm -rf ${P}/components_js
cp -r apps/nb/components_js ${P}

rm -rf ${P}/api-docs
cp -r apps/nb/api-docs ${P}
rm ${P}/api-docs/swagger.json

rm -rf ${P}/nb_vue
cp -r apps/nb/nb_vue ${P}

sudo service nb start
sudo service nb status